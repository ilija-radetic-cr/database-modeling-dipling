document:
    description: CRUD matrix synthesized from LLM operations and model evidence.
    functional_decomposition_file: functional_decomposition.yaml
    id: test_2_crud_matrix
    pipeline_version: "0.5"
    requirement_atoms_file: requirement_atoms.yaml
    source_units_file: source_units.yaml
    title: test 2 LLM Draft CRUD matrix
notation:
    C: create/insert
    D: delete
    R: read/select
    U: update
actors:
    - id: ACT-PROFESSOR
      label: Profesor
      description: Profesor koji predaje ispit.
    - id: ACT-STUDENT
      label: Student
      description: Student koji bira ispite.
operations:
    - id: OP-SELECT-EXAM
      label: Izaberi ispit
      functional_area: FA-STUDENT-SELECTION
      functional_pattern: create_relationship
      actor: ACT-STUDENT
      source_atoms:
        - RA-003
        - RA-004
      source_units:
        - RAW-SU-001
        - RAW-SU-003
      description: Student bira ispit, sto zahteva evidentiranje veze izmedju studenta i ispita.
matrix:
    - entity: ENT-EXAM
      table: exam
      operations:
        OP-SELECT-EXAM:
            - R
      rationale: CRUD row synthesized from LLM operation and model evidence overlap.
    - entity: ENT-STUDENT
      table: student
      operations:
        OP-SELECT-EXAM:
            - R
      rationale: CRUD row synthesized from LLM operation and model evidence overlap.
    - entity: ENT-PROFESSOR
      table: professor
      operations:
        OP-SELECT-EXAM:
            - R
      rationale: CRUD row synthesized from LLM operation and model evidence overlap.
    - entity: ENT-STUDENT-EXAM
      table: student_exam
      operations:
        OP-SELECT-EXAM:
            - R
      rationale: CRUD row synthesized from LLM operation and model evidence overlap.
    - entity: ENT-PROFESSOR-EXAM
      table: professor_exam
      operations:
        OP-SELECT-EXAM:
            - R
      rationale: CRUD row synthesized from LLM operation and model evidence overlap.
coverage_checks:
    - id: all_entities_have_crud_rows
      note: Each emitted entity has one CRUD matrix row.
      status: passed
